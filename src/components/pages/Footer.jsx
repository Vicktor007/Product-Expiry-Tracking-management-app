import React from "react";

export default function Footer({ classes }) {
  return (
    <footer className={`mt-4 text-center ${classes}`}>
      {" "}
      <a href="#">v1.01</a>
    </footer>
  );
}
